# Changelog fragments

> **Status:** live
> **Authority:** canonical for what this folder holds

One Markdown fragment per operator-visible change, harvested into
`CHANGELOG.md` at release and deleted. The format is
`Agentic-Control-Scheme\skills\changelog\FRAGMENT.md`. Preview the
unreleased section with `python <ACS>\bin\changelog.py preview --root .`.
