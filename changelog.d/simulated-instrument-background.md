category: minor

A realistic instrument background (environmental, sample and detector contributions) can be added on top of the simulated counts from a new dialog. It is off by default, so existing scans look exactly as before until you turn it on.
