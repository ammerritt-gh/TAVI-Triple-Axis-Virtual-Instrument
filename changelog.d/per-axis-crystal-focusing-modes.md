category: minor
notice: A bending radius outside a crystal's mechanical range is now refused instead of being rounded to the nearest allowed value, and every scan result records the radius each point actually ran with.

Each bending direction of a crystal can now be set independently to auto-focus, held at a fixed radius, or driven by a scan, and the analyser's second focusing axis is a real setting rather than a hidden fixed value.
