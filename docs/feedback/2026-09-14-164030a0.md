# Feedback — TAVI, 2026-09-14

**Session:** 164030a0-d540-4b28-b7d8-38e581b8271e
**Host:** Claude Code
**Work:** Release 1.3 audit blockers (GUI energy-edit crystal branches; API and GUI default angles), PR #39 merged

## Organisation
- The audit's reproducers under `docs/audits/repro/release-1-3/` copy the working tree to TEMP and run there, so they exercise the current checkout and exit 0/1 cleanly. They worked first time from both the main checkout and the worktree. Fine.
- The controller's reference state (Al (2,0,0), 14.7 meV, Kf fixed) was hard-coded as PUMA angle literals in three places: the API launch defaults, the GUI startup defaults, and the saved-parameter loader plus its malformed-number normaliser. The audit named the first; the Codex critic found the second; the external reader found the third. Now one solver (`_reference_angles` in `TAVI_PySide6.py`) feeds all of them. Latent risk that remains: any new writer of mtt/stt/omega/att must go through it; there is no guard test for a fourth copy. Open.

## Operator and agent surface
- `tests/test_controller_is_instrument_agnostic.py` forbids the string "PUMA" anywhere in `TAVI_PySide6.py`, comments and docstrings included. A docstring that named PUMA as the old default failed the full suite once and cost a repair commit. Bit this session, fixed. Smallest edit: one line in AGENTS.md's Verification section saying the controller may not name any instrument, even in comments.
- Tests that construct a real controller read the operator's `config/parameters.json` (gitignored, differs between the main checkout and a worktree). A test that compares GUI state to defaults must call `set_default_parameters()` first, or take its expectations from the launch state rather than live widgets. Bit this session twice (verifier advisory, then the reader's P3 on the next test); both fixed. This is the already-ledgered "test configuration isolation" entry 12.
- The full suite ran four times at 114 to 118 s wall each (measured), 1260 tests at the end, one skip for the Pb phonon map that a worktree lacks. Fine.

## Agent instructions
- AGENTS.md's Verification section says run the full suite through `micromamba run`; it does not say that a targeted red/green check of a single new test file should use `-p no:cacheprovider` in a worktree. The `.pytest_cache` ACL trap is documented; the flag worked and no time was lost. Fine.
- AGENTS.md does not mention `_reference_angles` or the single-authority rule for reference angles yet; the "Before You Touch X" table row for API scan jobs lists `_api_field_map` and `submit_scan_job` but not `build_api_launch_state`, which is where the energy-to-angle derivation now lives. Latent; open.

## Deferred on purpose
- No dedicated test for the saved-parameter load path's default angles; it reuses the tested solver. Said in the PR.
- The GUI startup default writer's old literals were transient (construction re-solves from Q), so that hunk has no red test; kept for single authority. Said in the PR.
