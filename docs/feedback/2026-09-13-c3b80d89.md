# Feedback — TAVI, 2026-09-13

**Session:** c3b80d89-c843-4a30-b0fb-c71325755a60
**Host:** Claude Code
**Work:** Branch (iv) of the audit harvest: a zero two-theta is a marked direct-transmission point; PR #37 merged, ledger entry 13 cleared, entries 14 and 15 opened

## Organisation

Fine. The audit ledger, its reproducer folder and the board carried
everything a fresh session needed; the pinned board section for entry 13
was accurate to the commit and was deleted by the commit that landed it.

## Operator and agent surface

**The test suite is red in every fresh worktree because `CLAUDE.md` is
gitignored. Bit this session (measured: 1 failed on the first full run in
the worktree, green after copying the file in).** `.gitignore` lists
`CLAUDE.md` under "agent files", and `tests/test_documentation.py` (added
by PR #36) runs the shared checker, which wants the memory section
reachable through that file. A worktree therefore fails a test the repo's
own docstring says must never go red for an environmental reason, and a
fresh clone of the public mirror does the same. Filed as ledger entry 15
and as a memory line in `AGENTS.md`; still open. Smallest edit: track the
one-line `CLAUDE.md` (it is `@AGENTS.md`), or teach the checker that
`AGENTS.md` alone satisfies the rule.

**A multi-line `python -c` through `micromamba run -n tavi-dev` never
reaches Python.** The environment's activation hook (the Visual Studio
vcvars batch `AGENTS.md` already calls noisy) re-parses the argument and
the shell tries to run `import`, `from` and the variable names as
commands. Cost: one wasted probe (measured); a script file in the session
scratchpad is the working shape. Latent for every session that probes the
environment. Smallest edit: one clause in `AGENTS.md`'s environment-traps
line.

**In a worktree session the Bash tool's guard refuses the `micromamba`
launcher when its path is computed** (`"$LOCALAPPDATA/..."`), and refuses
`git checkout -- <file>`, so the "from Git Bash use the absolute launcher"
advice in `AGENTS.md` §Verification does not hold there. The PowerShell
tool with `$env:LOCALAPPDATA` works, as does `git apply -R` on a saved
patch for a red-test check. Cost: four refusals across the session
(measured), each a retry. Latent.

The suite count moved 1208 → 1232 across the branch; a delegate flagged a
one-off discrepancy in its arithmetic that was the documentation test
flipping from failed to passed once `CLAUDE.md` was copied in. No cost,
but a baseline handed to a delegate should state which tests were failing,
not only the passed count.

## Agent instructions

**`docs/API_USER_GUIDE.md`'s self-contained operator block had drifted from
the endpoint section before this branch touched it.** The block still
listed `POST /validate` as taking only `parameters`, `force` and
`background` while the endpoint section documented the same; this branch
added `engine`, `seed` and `noiseless`, updated the endpoint section, and
the external reader caught that the paste-in block had not followed.
Fixed on the branch. Latent pattern: the guide carries the same contract
twice (a paste-in brief and the reference), and an edit to one is not an
edit to the other. Smallest edit: a comment beside the brief naming the
sections it mirrors.

`AGENTS.md` was otherwise accurate: the serial-run rule, the console-window
trap and the worktree launcher path all held. The new memory lines record
the worktree `CLAUDE.md` copy and the untracked Takin clone.

## Deferred on purpose

All on the board (`WIP.md`, audit-ledger section) after this session:
entries 9, 11, 12 (pinned from yesterday), 14 (analytic engine evaluates at
the nominal, not the misaligned, orientation; reproduce first), 15 (the
gitignored `CLAUDE.md` above); and, named but not filed, the dry run's
`would_queue` for a partial deterministic body, ISAR not reading
`transmission_points`, the two skip-kind vocabularies, the collimation
value-validation gap and `p_float` accepting `"nan"`. The Takin clone in
`examples/` is untracked and unwritten-up by the operator's choice today.
