# Feedback — TAVI, 2026-09-13

**Session:** b1f47801-cfed-45f6-8fde-1790dfbd300e
**Host:** Claude Code
**Work:** two slices of the changelog and release program run from Agentic-Control-Scheme: the version constant, the changelog opt-in and thirty backfilled fragments for 1.3; PR #38 merged

## Organisation

Fine. The PR bodies since v1.2.0 were rich enough to draft a changelog from
(twelve merged PRs, first-parent), and the design documents named in them
answered what the bodies did not; the two PRs with empty bodies (#22, the
API server; #26, the reciprocal-space display) were resolved from
`git ls-tree` and their file lists.

## Operator and agent surface

- **`micromamba` is not on PATH for a subprocess.** A release preflight that
  ran the AGENTS.md test command verbatim through `subprocess.Popen(shell=True)`
  failed in 0.0 s with "'micromamba' is not recognized". `changelog.d/release.toml`
  now names the absolute launcher via `%LOCALAPPDATA%\micromamba\micromamba.exe`,
  which `cmd.exe` expands. Bit this session (one wasted run and a rerun,
  about ten minutes). AGENTS.md already says "from Git Bash use the absolute
  launcher"; the same is true from any process that is not an interactive
  shell.
- **The pinned installer never generates `components/Pb_dft_phonons.dat`**
  (gitignored, 150 MB, `tools/make_pb_assets.py`), so a fresh install from
  the installer lists the lead phonon sample and fails to load it, and the
  one test that needs the file skips. Found by the external reader, not by
  any run here. Latent until the 1.3 release; pinned on the board as a
  prerequisite.
- The full suite under the release preflight ran green in 119 s (measured),
  one suite at a time, through `micromamba run`. The activation hook's
  vcvars output is as noisy as AGENTS.md warns.
- A fresh worktree's documentation test was red until `CLAUDE.md` was
  tracked (audit entry 15), which this session did; the memory line that
  told agents to copy the file in by hand was deleted with it.

## Agent instructions

Fine. The test-run paragraph was right in every detail that mattered
(`-ra` for the gitignored dispersion data skip, one suite at a time, the
absolute launcher from Git Bash).

## Deferred on purpose

- Two commits since v1.2.0 have no changelog fragment pending the
  operator's word: "Cleaned up GUI update boxes hints" and the env-setup
  script repair. On the board under "Release 1.3: prerequisites".
- Audit ledger entry 14 (the analytic engine ignores a hidden
  misalignment) stays open; the analytic-engine fragment says
  "approximate" until it closes.
