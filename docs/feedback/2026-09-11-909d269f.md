# Feedback — TAVI, 2026-09-11

**Session:** 909d269f-7f6c-4765-873c-09db834b8878
**Host:** Claude Code
**Work:** T2 on branch `crystal-bending-generality`, fifteen commits, not merged, no PR opened

## Organisation

**Stored — a model-status document understated a defect and that is what made
it look cosmetic (latent until read, fixed this session).**
`instruments/panda/MODEL_STATUS.md` carried a deferred-gap row stating that the
curvature defect was confined to the GUI's advisory "Ideal:" labels and that
"**Scans and the API launch path are unaffected** — they take the radii from
`scan_config`, which is correct." That was wrong when it was written. The
launch path was affected: the radii reaching the simulation came from a
hard-coded copy of one instrument's focusing formula, for every instrument.

The cost is not hypothetical — that sentence is why the work was initially
scoped as a GUI wart. Reading it and then checking the code against it is what
turned a cosmetic follow-up into a fifteen-commit branch that changed emitted
geometry on three instruments.

Related, and also fixed: six documents across `docs/` and the instrument
packages described `calculate_crystal_bending` as live code after it was
deleted, and `docs/INSTRUMENT_AUTHORING.md` instructed a new instrument author
to implement it and to apply the curvature branch sign per plugin. Both were
accurate when written; the branch falsified them.

## Operator and agent surface

**Stored — two pytest runs at once corrupt each other through a shared config
file (bit this session, ~10 minutes).** Running a targeted test file while a
full suite was in flight produced three failures in
`tests/test_parameters_persistence.py` that do not reproduce when the file runs
alone (20 passed). The repository already warns against concurrent runs, but
the warning in `TODO.md`'s housekeeping note names only the API server port and
`test_api_server.py` / `test_api_validation_schema.py`. The persistence tests
share `config/parameters.json` and fail the same way. Roughly ten minutes went
into diagnosing a regression that did not exist. Smallest edit: extend that
existing warning to name the persistence tests and the shared config file, so
the reason to serialise reads as general rather than port-specific.

**Stored — a fresh worktree silently loses a test (latent, worked around this
session).** `components/Pb_dft_phonons.dat` is a gitignored 143 MB build
product. A worktree created with `git worktree add` does not have it, and
`tests/test_dispersion_map.py` is guarded by
`@pytest.mark.skipif(not _PB_MAP.is_file(), ...)` — so it **skips silently** and
the suite still reads green with one fewer test. Hardlinked from the main
checkout this session, which kept the count honest at 0 skipped. Anyone making
a worktree without knowing this gets a quieter suite than they think.

**Stored — the documented test invocation does not work from Git Bash (bit this
session, minor).** `micromamba run -n tavi-dev python -m pytest tests -q`, as
written in `TODO.md`, fails with `micromamba: command not found` — it is not on
PATH in that shell. The working form is the absolute path,
`"/c/Users/AMM/AppData/Local/micromamba/micromamba.exe" run -n tavi-dev ...`.
One failed run before noticing.

**Fine, and worth keeping — the McStas smoke-run console guard.**
`docs/INSTRUMENT_AUTHORING.md:115-118` states that a smoke script runs outside
pytest, does not inherit the repo-root `conftest.py` guard, and must call
`install_no_window_guard()` before touching McStasScript or every instrument
construction opens a console window. Four compiled smoke runs were driven this
session with no console windows. The document said exactly what was needed,
where it was needed.

## Agent instructions

**Stored — this repository's `AGENTS.md` was never loaded (latent, general).**
All work happened here, but the session's *primary* working directory was a
different repository, so that repository's `CLAUDE.md` and `AGENTS.md`
auto-loaded and TAVI's own 283-line `AGENTS.md` did not. It was never read at
any point in a full session of work in this repository.

Nothing went wrong as a result, and that was luck. Key Design Decision #3 in
the unread document reads "**PUMA instrument construction belongs in
`instruments/puma/model.py`.** Do not duplicate McStasScript component setup in
GUI code" — which is precisely the defect the branch removed: PUMA's focusing
formula duplicated in the GUI controller and again in the widget-free API
equivalent. The repository had already written down the principle its code
violated, and the session happened to arrive at the same answer without reading
it.

This is not fixable inside this repository; it is a property of a session whose
primary working directory is a sibling repository. Recorded here so that a
later reader knows the branch was built without its floor in context.

## Deferred on purpose

- **The last four commits have not been seen by an external reviewer.** Eight
  functional defects from two external pre-PR rounds are all fixed, but the
  commits that fixed the final four were not re-reviewed — the operator ended
  the session at handoff and asked for no further review rounds. Stated at the
  top of `docs/HANDOFF-crystal-bending.md`.
- **A non-numeric radius typed into a GUI field blocks the run and tells the
  operator nothing** — `get_gui_values()` returns `None` for any unparseable
  field and the launch assembly bails, so the run quietly does nothing with no
  message. Pre-existing, unrelated to curvature, deliberately not widened into.
- **IN12's provisional 1.7 / 0.5 m monochromator minima, and PANDA's and IN12's
  analyser vertical radius**, remain unsourced and still want an instrument
  scientist. Recorded as such in their model-status documents, not closed.
- **No angle-dependent bender travel.** `TAS_Instrument.curvature_limits` is
  the seam for it; nothing in the tree needs it yet.
- **PUMA's arm lengths (L1–L4) stay provisional.** The operator's 2026-09-11
  review with the instrument scientist covered the bending limits and the fixed
  0.8 m analyser radius only, and the documents now say the arm lengths were
  explicitly not part of it.
