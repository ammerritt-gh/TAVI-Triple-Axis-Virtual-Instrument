# Feedback — TAVI, 2026-09-15

**Session:** c7ee4a11-9133-491c-950c-bd78189f4a0e
**Host:** Claude Code
**Work:** installer conda-only (PR #42), tests isolated from local config (two commits on main), installer ships conda-forge GCC (PR #43), release assets re-uploaded

## Organisation
1. Config path resolution was inconsistent across the code: three writers resolved `config/` from the working directory and two from the source file's location, and two readers (`tavi/api_server.py`, `tavi/mcstas_config.py`) from the source file while the matching writer used the working directory. A first census missed the readers and led to a wrong plan the critic caught. Fixed: `tavi/local_state.py` is now the single resolver. Bit this session; fixed.
2. `output/` still holds `pytest_tmp_reciprocal*` folders with broken ACLs from earlier test runs (cannot be listed without an elevated shell). Latent; the new tripwire lists `output/` top-level names, so any future test that writes there fails the session. Not fixed; needs `takeown`/`icacls` as `AGENTS.md` already says for `.pytest_cache`.
3. Design document sections in `installer/TAVI_Windows_Installer_Uninstaller_Design_Document.md` have no headings for the compiler, `vcvars` or MPI SDK topics; the executor had to add "superseded" notes inline at four places. Latent, cosmetic.

## Operator and agent surface
1. The test suite wrote the operator's real `config/view_layout.json` (all docks hidden) on 2026-09-14 during the release run, and had been rewriting `parameters.json` and `runtimes.json` on every run. Bit the operator (GUI opened with every panel hidden). Fixed: `TAVI_CONFIG_DIR` set by `conftest.py` to a temp copy, plus a session tripwire that fails the run on any change to real `config/` or `output/`. The tripwire's false-positive case (a GUI open beside the suite) is named in its message.
2. Two tests opened repository files by bare relative path (`tests/test_editable_number_format.py`, `tests/test_setup_tavi_dev.py`); fixed to `__file__`-based paths.
3. The dev environment's McStas config (`tavi-dev`, `share/mcstas/tools/Python/mccodelib/mccode_config.json`) is a hardlink shared with the package cache and the `tavi` environment; an installer run against a throwaway environment rewrote it. Restored from the cached `.conda` archive. Any future script touching a file under an environment must unlink first. Bit this session; fixed in the installer; latent for every other script.
4. `run-tavi-dev.bat` and `setup-tavi-dev.bat` still depend on Visual Studio and the noisy `vcvars` activation; the installed environment no longer does. Pinned on the board.
5. Suite timing: 1261 tests in about 110 s wall (measured, twice), inside the routine budget.

## Agent instructions
1. `AGENTS.md` Verification §3 said concurrent runs contend for `config/parameters.json`; true until this session, now only the API port contends. Updated.
2. `AGENTS.md` "External runtime dependency" now names the two compilers (installed env: conda-forge GCC; dev env: MSVC). Updated.
3. Nothing in `AGENTS.md` or `tests/README.md` warned that `micromamba run ... cmd /c` from the agent's Bash tool can hang, or that the agent tool shells set `NoDefaultCurrentDirectoryInExePath=1`, which makes every `mcrun` launch from a tool shell fail with "is not recognized". Latent for the next session that runs McStas from a tool shell; worth a line in `tests/README.md` or `AGENTS.md` Environment traps.
4. The memory line about the Bash guard refusing "git" in any command text held and was needed all day; the scratchpad and repo paths contain "Github".

## Deferred on purpose
- `DEFAULT_MPI_COUNT = 30` fails on Linux hosts with fewer cores (Open MPI refuses to oversubscribe); pinned on the board for 1.3.1 with a proposed core-count default.
- POSIX installer untouched by the GCC change and still never run on either platform.
- `installer/WINDOWS-install-TAVI.bat` (unversioned, documented as unmaintained) and the v1.1/v1.2 installers keep the pip step and the Visual Studio check as historical artefacts.
- The council's rejected "MinGW/MSVC double-return ABI" objection was rejected on reasoning (both use the Microsoft x64 convention); the physics cross-check on PSI_DMC_simple (12 monitors within 3 σ) is the evidence, and the PUMA point chosen for the cross-check was nearly dark, so a bright-point PUMA comparison has not been done.
