# Feedback — TAVI, 2026-09-11

**Session:** 09bdc537-9c85-4904-9d76-580b1454434a
**Host:** Claude Code
**Work:** T3 landing program for `crystal-bending-generality`; PR #32 merged; one T1 docs commit on `main`

## Organisation
- **The transcript and feedback-record convention is invisible from inside this repository (latent).** `docs/transcripts/` (verbatim session records) and `docs/feedback/` (these records) are a workspace convention written in Agentic-Control-Scheme's AGENTS.md. TAVI's own AGENTS.md does not mention either, so a session working here with untracked files in both folders had to look in a sibling repository to learn they are meant to be committed. Smallest edit: one line under "Project Structure" in AGENTS.md naming both folders and the rule that transcripts are never edited.
- **A workstream handoff on a branch is unreachable from `main` until merge (bit, one operator decision).** `docs/HANDOFF-crystal-bending.md` was committed on the branch by the previous session; the floor says handoffs live on `main`. Resolved by the operator (leave it, it lands at merge), and it has now landed. The next branch-scoped workstream will face the same choice.

## Operator and agent surface
- **`tests/test_controller_is_instrument_agnostic.py` refuses the literal word "PUMA" anywhere in `TAVI_PySide6.py`, comments included (bit this session, four full-suite runs, ~7 min remembered).** Two executors, in two slices, wrote "PUMA" in a code comment explaining a nested-mirror-optic rule, saw the guard fail on the full run, reworded, and ran again. The guard is right; the trap is that nothing tells an author before the 100 s run. Smallest edit: a sentence in `tests/README.md` (and in AGENTS.md's "Anti-Patterns") saying the controller file may not contain an instrument name even in prose, and what to write instead ("the active instrument", "a fitted mirror optic").
- **There is no committed smoke-run script; every session rewrites one (bit, ~5 min of executor time).** `docs/INSTRUMENT_AUTHORING.md` step 8 describes the compiled smoke run and the console-guard requirement, and the handoff table records numbers from it, but the script that produced them lives in a scratchpad each time. This session's copy drove the production path (`build_api_launch_state` + `run_simulation` with the McStas engine, output redirected outside the tree) for all four instruments in under 20 s total. Smallest edit: commit it as `tools/smoke_runs.py` with the output directory as its one argument.
- **Suite time grew from ~92 s to ~100 s this session (measured: 1074 -> 1104 tests).** Still within the routine budget; noting the direction. Every new test is an offscreen-Qt controller construction (2-3 s each file); a shared controller fixture per instrument would hold the line when the next thirty land.
- **The `tavi-dev` activation noise reaches `micromamba run` output too (bit, one turn).** AGENTS.md's environment-traps line describes the MSVC `vcvars` echo for activation; it also swamps `micromamba run ... | Select-String` in the PowerShell tool. Redirecting pytest output to a file and grepping it is the working form; worth one clause in that line.
- Fine, and worth keeping: the absolute micromamba launcher and the `-ra` skip reporting written into `tests/README.md` this session (commit c868f080 on `main`) were used by five executors with no mis-invocations.

## Agent instructions
- AGENTS.md's verification status line said 973 passed from 2026-09-10 and now says 1104 from 2026-09-11 (fixed, commit 5b368323). It will be stale again after the next merge; a dated line is the right shape, but the number goes stale the day it is written.
- Fine otherwise. Key Design Decision 3 (no duplicated physics setup in GUI code) was the rule the whole branch enforced, and it was read this time.

## Deferred on purpose
- Everything pinned is on the board: TODO.md rows under "API polish", "GUI" and "Housekeeping" added in 5b368323, each pointing at the ledger at the end of `docs/PLAN-crystal-bending-landing.md`. Nothing deferred is outside those rows.
- The `TAVI-wt-before` and `TAVI-wt-bending` worktrees and the merged branch were removed at closeout; the main checkout is the only tree.
